[package]
name = "depth-libinfer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "FoundationStereo depth via libinfer TensorRT engines (sm_120, FP16), device IO only"

[dependencies]
thiserror = "2"
