[package]
name = "tsdf-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The common TsdfBackend interface every fusion arm implements"

[dependencies]
thiserror = "2"

